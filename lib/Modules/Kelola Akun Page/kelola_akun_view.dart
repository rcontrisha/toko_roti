import 'package:flutter/material.dart';

class KelolaAkun extends StatelessWidget {
  const KelolaAkun({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Kelola Akun Screen',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
